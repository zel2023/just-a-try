# 2026-04-23

## 已完成
**简要概括**
- 明确了 MVP 需求：多房间 + OpenAI 兼容接口 + AI 自动回复 + 内存历史 + 公网部署方向。
- 初始化工程结构（目录与 `.env.example`）。
- 完成 WebSocket 聊天后端骨架（广播/在线列表/系统消息/历史下发）。
- 接入 AI（OpenAI 兼容，按房间串行生成，支持自动回复与 `/ai`）。
- 实现前端单页：昵称/房间、消息流、在线用户与基础样式。
- 增加部署与运行：依赖文件、Docker/Compose、后台运行脚本、README 快速开始。
- 已归档 `Done/chat-ai-web.md` 并删除 `TODO.md`（MVP 阶段收尾）。

**稍微详细**
- 约定房间通过前端输入加入；后端按房间隔离在线连接与历史上下文。
- AI 默认值（`OPENAI_BASE_URL`/`OPENAI_MODEL`）可在 `.env` 配置；`API Key` 与 `Base URL` 支持用户在网页端输入（服务端不内置 Key，避免公网刷 Key 成本）。
- 新增 `FastAPI` 服务：`/ws` 处理 WebSocket，按房间广播消息；同时广播 presence（在线用户列表）与 system（加入/离开）。
- 新增 AI worker：每个房间一个队列，串行调用 OpenAI 兼容 `chat/completions`；支持 `/ai ...` 或 `@ai ...` 强制触发。
- 新增 `static/` 前端：连接 `/ws`，渲染 history/system/chat/presence，并在右侧面板显示在线用户。
- 新增 `requirements.txt`、`Dockerfile`、`docker-compose.yml`、`scripts/run_server_bg.py`，并在 `README.md` 补齐最小使用说明。
- 按约定将 `TODO.md` 迁移到 `Done/chat-ai-web.md` 并删除原 `TODO.md`。

## 下一步
**简要概括**
- 部署上线：GitHub + Render 自动部署（先用 Render 默认域名，后续可再绑域名）。

**稍微详细**
- 初始化 Git 仓库并推送到 GitHub（公开仓库）。
- 在 Render 上创建 Web Service 并打开 Auto Deploy，实现 push 后自动上线。
